# Runtime Scripts

These scripts are used internally by the Zinga to manage the local Mopidy/Icecast runtime.

Main scripts:

- `setup.sh`
- `start.sh`
- `stop.sh`
- `restart.sh`
- `verify.sh`

Logs:

- `/tmp/mopidy.log`
- `/tmp/ffmpeg_icecast.log`
